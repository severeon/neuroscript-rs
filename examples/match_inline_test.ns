# Test inline match syntax only
neuron DimensionRouterInline(out_dim):
  in: [batch, in_dim]
  out: [batch, out_dim]
  graph:
    in -> match: ->
      [batch, d] where d > 1024: Linear(d, 512) -> Linear(512, out_dim)
      [batch, d] where d > 256: Linear(d, out_dim)
      [batch, d]: Linear(d, out_dim)
    -> out
