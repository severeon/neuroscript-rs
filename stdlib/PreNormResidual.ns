neuron PreNormResidual(dim, dropout_rate):
    in: [*shape, dim]
    out: [*shape, dim]
    graph:
        in -> (main, skip)
        main ->
            LayerNorm(dim)
            Linear(dim, dim)
            Dropout(dropout_rate)
            processed
        (processed, skip) -> Add() -> out
