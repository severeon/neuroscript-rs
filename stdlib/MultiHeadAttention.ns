# Multi-Head Attention
#
# Core mechanism of transformers. Computes attention over sequences
# using multiple parallel attention heads.
#
# Note: This is a simplified version using attention as primitives.
# Full attention with head splitting would require more complex composition.
# See primitives.ns for ScaledDotProductAttention and MultiHeadSelfAttention
# neuron definitions.
