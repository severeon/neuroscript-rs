---
source: tests/integration_tests.rs
expression: formatted
---
=== Neurons ===

neuron ParallelFFN(dim):
  inputs:
    default: [*batch, dim]
  outputs:
    default: [*batch, dim]
  graph:
    in -> FFN#0(dim, (dim * 2))
    FFN#0(dim, (dim * 2)) -> out
